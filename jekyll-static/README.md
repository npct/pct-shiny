To update static pages make edits to files in this folder, then run the following commands (requires Ruby and the bundle package to be installed):

```
cd jekyll-static
bundle install
jekyll s -d ../regions_www
```

